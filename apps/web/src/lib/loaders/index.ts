/**
 * Loader Utilities
 *
 * Shared route loader logic for consistent SSR hydration
 */

export {
  type ChatThreadLoaderData,
  createEmptyLoaderData,
  fetchThreadData,
  type ProjectChatThreadLoaderData,
} from './chat-thread-loader';
